# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'HalaanTaskPresentation' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HalaanTaskPresentation

  target 'HalaanTaskPresentationTests' do
    # Pods for testing
  end

end
